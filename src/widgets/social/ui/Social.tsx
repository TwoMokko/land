'use client'

import styles from './Social.module.scss'

export function Social({ idSection }: { idSection: string }) {
    return <section id={idSection} className={styles.social}>
        social
    </section>
}